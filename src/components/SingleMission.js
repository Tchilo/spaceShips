/* eslint-disable */
import React from 'react';

function SingleMission({ mission_name, description }) {
    return (
        <div>
            <h2>{mission_name}</h2>
            <p>{description}</p>
        </div>
    );
}

export default SingleMission;
